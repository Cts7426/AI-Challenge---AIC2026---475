# Dependencies của backend. Cài: pip install -r backend/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.40   # chỉ cần khi LLM_BACKEND=api
# ⚠️ THIẾU cho tới 19/08 dù `backend/llm/adapter.py` import nó và toàn bộ 14 lệnh
# gọi LLM trong lịch sử cache đều đi qua backend này. Hậu quả: máy mới clone về
# không collect nổi `tests/test_llm_adapter.py`, và `python -m pytest tests` DỪNG
# HẲN với "2 errors during collection" — tức cả bộ test không chạy, không phải
# chỉ thiếu một file.
google-genai>=1.0   # chỉ cần khi LLM_BACKEND=gemini
elasticsearch>=8.13,<9   # client ES — giữ major 8 khớp server trong docker-compose
pymilvus>=2.4    # client Milvus
numpy>=1.26      # đọc file .npy CLIP features

# D0.2 + D3.1 (Minh Hoàng): đọc video_info / shots / frame_map .parquet
# ⚠️ Đã bị gỡ nhầm ở commit 5784ff8, thêm lại 07/08. Thiếu hai dòng này thì
#    backend/export/ và backend/slot/ không chạy được trên máy mới clone.
pandas>=2.2
pyarrow>=15.0

# Test — D0.2 và D3.1 đều bắt buộc unit test
pytest>=8.0

# 2 gói dưới nặng (~vài trăm MB) — chỉ cần cho query encoder (Task 2.1+)
# torch bản CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
open_clip_torch>=2.24   # CLIP — model/pretrained đọc từ data/config/clip_model.py

# CHỈ CÀI TRÊN COLAB/KAGGLE (job preprocessing — KHÔNG cài máy dev 16GB):
#   paddlepaddle-gpu + paddleocr   → preprocessing/ocr_job.py  (Task 4.1)
#   faster-whisper                 → preprocessing/asr_job.py  (Task 4.2)
