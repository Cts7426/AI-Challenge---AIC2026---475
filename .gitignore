# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/
.pytest_cache/

# Data cuc bo (khong day len git)
data/sample/*
!data/sample/.gitkeep
*.npy
data/raw/
data/derived/keyframes/
data/derived/audio/
data/derived/verify_frames/
data/derived/full_verification.parquet
data/derived/full_verification_test.parquet
data/derived/full_verification_parts/
reports/recovered_frames/
reports/black_frames_list.txt
cleanup_inventory.md
scratch/

# Docker volumes neu lo bind-mount
volumes/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# Claude Code — chi push file dung chung cho team
CLAUDE.local.md
CLAUDE.md*Zone*
.claude/*
!.claude/skills/
!.claude/launch.json
# settings.json chứa hook chạy pytest + permissions dùng chung cho cả team
# (không chứa secret). settings.local.json mới là chỗ để override riêng máy.
!.claude/settings.json
.claude/settings.local.json

# Zone.Identifier (Windows downloaded files)
*Zone.Identifier

# Old nested folder (da flatten)
AI-Challenge---AIC2026---475-main/

# File nop sinh ra khi bam Nop (Task 3.2)
submissions/

# Meta cua index vector: sinh khi nap tren TUNG may (duong dan, commit, gio khac nhau)
data/derived/*.meta.json

# Cache LLM tren dia (sinh khi chay, moi may mot khac)
data/cache/
data/manifests/
learning/
reports/shot_preview/
data/derived/*

# Local backups
_local_backup_*/

# Env variables
.env
.env.*

# Dữ liệu lớn cục bộ (KHÔNG được đẩy lên git — vd .zip keyframe 46GB đã từng
# xuất hiện ở gốc repo, có thể phá git nếu lỡ `git add -A`). `data/derived/*`
# ở trên đã bao trọn mọi thư mục con của data/derived — giữ thêm các mục dưới
# đây vì chúng KHÔNG nằm trong data/derived/.
*.zip
backup_running/
data/derived/_audit/
data/derived/asr_parts/
data/derived/ocr_parts/
data/derived/samples/
data/derived/shots_parts/
data/derived/shots_parts_backup/
data/derived/real_metadata_temp.json
.tmp_*

# File làm việc của quy trình thi — `exam.py prepare` sinh lại mỗi buổi.
# KHÔNG commit: `exam_confirm.json` của đề cũ mà lọt vào repo thì `finalize`
# có thể nhét đáp án đợt trước vào bài nộp đợt này. Bản dịch/anchor đáng giữ
# thì chép sang `dev_set/queries/round1_kis_plans.json` (đã được theo dõi).
dev_set/queries/exam_*.json
dev_set/queries/exam_*.jsonl
dev_set/results/run_*/
