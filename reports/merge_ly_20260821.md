# Merge nhánh Lý + chiều sâu TRAKE — 21/08/2026

> **Minh Hoàng** · nhánh `integrate/ly-20260821` (tách từ `origin/main` `7e15ff3`)
> **Chưa push, chưa merge vào `main`.** Mọi số dưới đây đo qua ĐƯỜNG CHẠY THẬT
> (Milvus + Elasticsearch + `search()` + `allocate()` + `dev_set/tools/scoring`).

---

## 1. Kết quả

| | tune (23) | holdout (36) | dress25 (19) | TB có trọng số |
|---|---|---|---|---|
| `main` trước merge | 0.539 · R@1 0.130 · R@5 0.217 | 0.111 | 0.379 | 0.302 |
| **nhánh này** | **0.539 · R@1 0.261 · R@5 0.435** | **0.194** | **0.400** | **0.346** |

Quy ra số câu trên tune: **top-1 từ 3 → 6 câu, top-5 từ 5 → 10 câu.**

TRAKE (2 câu có ground truth): **0.508 → 0.575**.

Test: **624 pass**. 2 fail ở `tests/test_evidence.py` là **lỗi có sẵn trên `main`**
— hai test đó giả định chưa tải ảnh keyframe, mà máy này đã có 873 thư mục ảnh.
`app/evidence.py` và `tests/test_evidence.py` giống hệt `main`, merge không đụng vào.

---

## 2. Ba bộ đề, ba thiên lệch NGƯỢC nhau — vì sao phải đo cả ba

| Bộ | Cách dựng cửa sổ đáp án | Thiên lệch |
|---|---|---|
| tune (23) | 23/28 trùng khít biên shot | không ưu ái nhánh nào rõ rệt |
| holdout (36) | 60/63 có tâm đúng một keyframe **do BM25 chọn**, ±75 frame | **VÒNG TRÒN với nhánh text** |
| dress25 (19) | 19/24 trùng biên shot, mô tả do VLM viết **từ khung hình** | ưu ái nhánh vector |

**Hệ quả:** mọi kết luận thiên vị ocr/asr đo trên holdout đều bị thổi phồng — bộ đề
đang tự chấm chính công cụ đã sinh ra nó. Cấu hình đáng chọn phải đứng vững cả ba.

Cửa sổ TRAKE thì **28/28 trùng khít biên shot** → vòng tròn với `shots.parquet`, và
rộng 22–318 frame trong khi cửa sổ thật được ghi là <10 frame. Điểm TRAKE trên dev
dễ hơn thực tế nhiều.

---

## 3. Giải xung đột — nguyên tắc "Thạch thắng mọi tranh chấp"

Soi 4 commit gần nhất của Thạch (20/08 16:37 → 21/08 02:21), chủ đề rõ: **Q&A +
giải đường dẫn ảnh + API/health + hạ tầng chấm điểm**.

| File | Cách giải |
|---|---|
| `backend/tasks/qa.py` (Thạch đụng 3/4 commit) | **lấy nguyên `main`** |
| `backend/slot/allocator.py` (2/4, sửa cuối 02:21) | **lấy nguyên `main`** |
| `CLAUDE.md` | **lấy nguyên `main`** |
| `app/evidence.py` | tự động khớp = `main` |
| `.gitignore` | hợp cả hai |
| `data/config/search_weights.py` | **hợp thủ công** — ngoại lệ duy nhất |

**Hai file "ấm" của Thạch bị đụng, cả hai đều bắt buộc:**
1. `data/config/search_weights.py` — `RRF_K` sống ở đây. Chỉ sửa dòng 17 và nối
   hằng TRAKE vào cuối; **khối `QA_TEXT_FALLBACK_*` của Thạch không chạm một chữ**.
2. `run_minimal.py` — một dòng, sửa hồi quy **do chính thay đổi TRAKE gây ra**
   (`to_answers` giờ sinh tới `total` dòng nên phải truyền `args.answers`, không thì
   `--answers 50` ra 100 dòng và validator từ chối cả file).

Phần Q&A của Lý tách ra `reports/ly_qa_patch.diff` (`MAX_SHOTS_TRIED = TOP_K_SHOTS`,
`HIGH_CONFIDENCE_EARLY_STOP`, `_expand_within_video`) — **giao Thạch, chưa merge**.
Hai lý do: (a) đó là file nóng nhất của anh; (b) tối nay **không có
`ANTHROPIC_API_KEY`, không `GEMINI_API_KEY`, không Ollama** nên không kiểm chứng
được một dòng Q&A nào. Bản sửa đường dẫn ảnh của Lý cũng đã bị `main` thay bằng
`backend/common/frame_assets.py` — cùng bug, giải kỹ hơn.

---

## 4. Thay đổi duy nhất bị BỎ: `BRANCH_WEIGHTS`

Ở **cùng `RRF_K = 7`**:

| trọng số | tune Final | tune R@1 | holdout | dress25 |
|---|---|---|---|---|
| **0.7/0.6/0.6/0.4 (giữ)** | **0.539** | **0.261** | 0.189 | **0.400** |
| 1.0/1.0/1.0/0.8 (nhánh Lý) | 0.409 | 0.043 | 0.206 | 0.368 |

Mất **0.130 Final** và **0.218 R@1** trên tune (R@1 sụp 83%: 6 câu → 1 câu), thua cả
dress25, đổi lấy **+0.017** trên holdout — mà 1 câu trong 36 chỉ đáng 0.0056, nên
+0.017 nằm trong nhiễu, lại đo trên đúng bộ vòng tròn với nhánh text.

Thay đổi này vào ở commit `9b65b9f` lúc **01:14 sáng**, trong một commit tiêu đề
*"Add generated QA/TRAKE test queries and fix QA fallback image path"*. **Không có
lần đo nào** chạy với bộ trọng số đó — cả 4 run trong `dev_set/results/` đều snapshot
`0.7/0.6/0.6/0.4` — và nó ngược với chính `reports/holdout_teammate_check.md` §3
(*"ĐÃ THỬ tăng ocr/asr — LOẠI BỎ"*).

**Cơ chế:** ở K=7 mọi nhánh hạng-1 đều đóng góp `1/(7+1) = 0.125`. Nâng objects/ocr
lên 1.0 khiến hạng-1 của nhánh nhiễu (khớp nhãn "Person"/"Clothing") ngang giá hạng-1
của vector; metadata 0.8 nâng **mọi** keyframe của một video khớp tiêu đề lên 0.1 cùng
lúc. Đầu bảng bị lấp bởi rác — đúng chỗ R@1 sống.

### `RRF_K = 7` thì ĐÚNG — kiểm chứng độc lập

| K | tune F / R@1 / R@5 | holdout | dress25 |
|---|---|---|---|
| 60 (cũ) | 0.539 / 0.130 / 0.217 | 0.111 | 0.379 |
| 15 | 0.530 / 0.130 / 0.435 | 0.161 | — |
| 10 | 0.539 / 0.261 / 0.348 | 0.172 | — |
| **7** | **0.539 / 0.261 / 0.435** | **0.189** | **0.400** |
| 3 | 0.487 / 0.261 / 0.348 | 0.194 | — |

Vách rõ giữa K=10 và K=15. **Đính chính một chữ trong báo cáo cũ:** "không đánh đổi"
là không chính xác — K nhỏ mua đầu bảng và **bán đuôi**: tune R@100 tụt 0.870 → 0.739,
3 shot đúng bị đẩy khỏi top-100. Final hoà vì hai phần bù nhau. Vẫn là lựa chọn đúng,
nhưng đừng ghi là miễn phí.

---

## 5. TRAKE — chiều sâu dòng nộp

**Vấn đề (đo trên file nộp thật `dev_set/results/run_20260820_2101/`):**
`to_answers()` sinh đúng 1 dòng mỗi video → 100 dòng = **100 video khác nhau**. Mà
`R@k = R-Score cao nhất trong k dòng đầu`, nên khi video đúng đã ở hạng 1 thì 99 dòng
còn lại **không thể** cải thiện điểm.

- TR01: video đúng ở dòng 1, trúng 2/4 → R@1..R@100 đều 0.50, **Final đóng băng**
- TR02: video đúng ở dòng 5, trúng 2/3 → Final 0.533

Và phương án đúng **nằm sẵn trong rổ**: 2/3 vị trí bị DP chọn hụt vẫn có ứng viên đúng
trong rổ của chính vị trí đó.

**Sửa** (chỉ trong `backend/tasks/trake.py` — Thạch không đụng file này lần nào):
- `TrakeCandidate.alternatives`: các bộ N frame khác, mỗi bộ đổi **đúng một vị trí**
  sang ứng viên tốt kế tiếp, xoay vòng theo vị trí đáng ngờ trước
- `to_answers`: **20 dòng đầu giữ nguyên vẹn** như cũ → R@1/R@5/R@20 không thể tệ đi
  *về mặt cấu trúc*; phần còn lại xen kẽ phương án thay thế với dòng gốc
- `pad_answers`: bước dịch `+1 frame` → 60 frame. Hai ca trượt đo được đều là chọn
  nhầm **đúng một shot liền kề** (~45 frame), dịch 1 frame là rác

| bảng ngân sách | TR01 | TR02 | TB |
|---|---|---|---|
| không chiều sâu (cũ) | 0.750 | 0.267 | 0.508 |
| **`[(5,3),(95,1)]` ← chọn** | 0.750 | **0.400** | **0.575** |
| `[(1,12),(4,3),(95,1)]` | 0.750 | 0.333 | 0.542 |
| `[(100,2)]` / `[(100,1)]` | 0.750 | 0.267 | 0.508 |

⚠️ **TR01 không cải thiện ở bất kỳ bảng nào** — ứng viên đúng của vị trí 2 nằm ở
**hạng 11/12** trong rổ của chính vị trí đó, ngoài tầm mọi ngân sách hợp lý. Đó là lỗi
xếp hạng trong rổ (CLIP), chiều sâu không sửa được.
⚠️ **Cỡ mẫu 2 câu.** Đọc là "chiều sâu có tác dụng thật, mức vừa phải tốt hơn mức dồn",
không phải "đã tối ưu".
⚠️ **Đánh đổi đã biết:** mỗi dòng phương án chiếm chỗ một dòng gốc; video hạng 36–50 bị
đẩy từ R@50 xuống chỉ còn R@100.

---

## 6. Ba hướng đẩy KIS đã ĐO và BỎ

Không thêm dòng code nào — ghi lại để lần sau khỏi đo lại.

**a) Nạp thêm keyframe 1fps (`load_clip_local.py` của Lý) — tiền đề SAI.**
File ghi *"6/23 câu KIS không tìm thấy vì đúng shot chưa từng được embed"*. Kiểm:
**cả 28 cửa sổ tune đều có sẵn keyframe BTC bên trong**, kể cả 6 câu trượt (K01/K03/
K06/K08/K11/K18 có 1/1/2/1/2/1 keyframe đã nạp vector). Đúng shot **có trong index**,
CLIP chỉ xếp quá thấp. Đo thêm: chỉ **1/6** câu có keyframe 1fps trong cửa sổ vượt được
ngưỡng top-100. Không đáng chạy job encode 2,3 giờ.

**b) Nâng trọng số nhánh vector.** Chẩn đoán ban đầu có lý (nhánh vector xếp shot đúng
cao hơn hẳn kết quả hợp nhất: K09 vector=18 → hợp nhất 71; K13 vector=5 → 19), nhưng đo
ra TB **đứng yên 0.346** ở mọi mức 1.0–2.0, và nâng vector còn **làm tụt tune R@5**
0.435 → 0.304. Giữ nguyên.

**c) Hợp nhất hai thứ hạng K (K=7 cho đầu bảng, K=60 giữ đuôi).** Chạy đúng như thiết
kế — tune R@100 phục hồi 0.739 → 0.870 — nhưng rổ sâu hơn (1500 thay vì 500) tự nó đổi
thứ hạng từng nhánh và làm hỏng đầu bảng: R@1 0.261 → 0.217, R@5 0.435 → 0.348. TB
0.346 → 0.341. Trượt luật "thắng ≥2/3 bộ, không thua bộ nào quá 0.01".

---

## 7. Năm việc cho Lý / Thạch — nằm trong vùng của các anh, tôi chỉ báo

### 🔴 1. `keyframes.parquet` lệch hẳn với ảnh trên đĩa — Lý (B1.2)

```
keyframes.parquet  L21_V001 : frame 14, 43, 72, 100, 129 …  (lấy mẫu theo SHOT)
file thật trên đĩa L21_V001 : f0000000, f0000030, f0000060 … (bước cố định 30)
```

Chỉ dòng nào có `frame_idx` chia hết cho 30 mới tra ra file → **95,5% đường dẫn trong
`keyframes.parquet` trỏ vào file không tồn tại** (mẫu 3000 dòng: 135 file có thật).
Ảnh do một job cũ bước-cố-định sinh ra, parquet do job shot-aware sinh sau, không ai
trích lại ảnh. Không crash, không cảnh báo — panel trống và job đọc ảnh chạy rỗng.
`load_clip_local.py` đọc chính cột `path` này nên sẽ hụt ~95% đầu vào.

### 🔴 2. `search()` không có đường lui khi `llm()` chết — Thạch
Thiếu `query_en` là bắt buộc gọi `llm()` dịch, không có nhánh dự phòng. Gặp đúng ca
này tối nay: hết tiền API → `trake_search()` trả **0 video ứng viên** cho cả 2 câu.
`run_minimal.py` mang tiếng là kịch bản dự phòng nhưng chết theo. Trước 19:30 nên có
ít nhất một đường: dùng thẳng `query_vi` cho nhánh vector, hoặc bắt buộc điền
`query_en` tay trong UI.

### 🟡 3. Milvus `ef = max(128, limit)` mất tới 25% top-100 thật — Thạch
`backend/retrieval/search.py:105`. Đo trên 6 câu, so với brute-force chính xác toàn bộ
177.321 vector: **75%, 83%, 96%, 97%, 99%, 100%**. Đường KIS ở `pool=500` có `ef=500`
nên đỡ; chỗ nào gọi `top_k` nhỏ thì dính.

### 🟡 4. Hai nguồn `frame_idx` cho cùng một keyframe
`clip_row_index.parquet` (nạp vào Milvus, TRAKE dùng) và `frame_map.parquet`
(slot allocator dùng) lệch nhau ở **81/177.321** keyframe. Nhỏ, nhưng là hai nguồn sự
thật cho đúng con số quyết định điểm.

### 🟡 5. Bộ đề TRAKE tổng hợp có dấu vết bộ sinh máy móc
`STR01`–`STR06`: sự kiện đầu tiên của **cả sáu** câu rơi vào đúng `#s0010`, các sự kiện
sau cách đều tới cuối video. `TRAKE_MIN_BLEND_LAMBDA` và
`TRAKE_ASR_CONTEXT_BONUS_WEIGHT` được tune trên "8 câu" mà **6/8 là bộ này**. Đã ghi
chú trong `search_weights.py`: giữ vì vô hại, không phải vì đã chứng minh.

---

## 8. Bàn giao

Hai commit trên `integrate/ly-20260821`:
- `bcee061` merge nhánh Lý, giữ `BRANCH_WEIGHTS` đã đo của `main`
- `88238f2` chiều sâu dòng nộp TRAKE

**Chưa push, chưa merge vào `main`.** Kiểm rồi tự quyết lúc nào ff `main`.
